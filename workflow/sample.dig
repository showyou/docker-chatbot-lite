timezone: Asia/Tokyo

_export:
    docker:
        image: docker-chatbot-lite_bot

schedule:
    minutes_interval>: 1
    skip_delayed_by: 1m

+setup:
    echo>: start ${session_time}

+main:
    sh>: cd src && python3 crawler.py

+teardown:
    echo>: finish ${session_time}
